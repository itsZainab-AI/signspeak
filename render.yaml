services:
  - type: web
    name: signspeak
    env: docker
    dockerfilePath: Dockerfile
    startCommand: "uvicorn src.app:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: DEPLOY_MODE
        value: hosted
      - key: GROQ_API_KEY
        sync: false
